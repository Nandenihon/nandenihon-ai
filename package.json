{
  "name": "nandenihon",
  "version": "0.1.0",
  "private": true,
  "packageManager": "npm@10.9.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "type-check": "turbo type-check",
    "test:enrollment": "node --experimental-strip-types --test packages/database/test/enrollment-domain.test.ts",
    "dev:landing": "turbo dev --filter=landing",
    "dev:student": "turbo dev --filter=student-portal",
    "dev:admin": "turbo dev --filter=admin-portal"
  },
  "devDependencies": {
    "turbo": "^2"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1"
  }
}
